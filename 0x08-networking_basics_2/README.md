# Networking basics #1

* what are hosts file
	A file that serves as a small gateway between you and the web.

* Getways
	is a device used to connect two different network esp a connection to the internet.

* Linux hosts file 
	is a plain text file that all OS use to translate hostnames(URLs) into IP address

DNS[Domain Name System] > hostfiles

to view host file on linux 
vi /etc/hosts

netcat / nc
telnet
cut
ifconfig
